import { SearchFocusDirective } from './search-focus.directive';

describe('SearchFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFocusDirective();
    expect(directive).toBeTruthy();
  });
});
